[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Image_Kit"
version = "0.1.3"
description = "A simple image edit"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Brandon Chew", email = "chewbrandon9911@gmail.com"}
]
dependencies = []

[project.scripts]
Image_Kit = "Image_Kit:run"
