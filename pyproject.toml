[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "Image-Kit"
version = "0.0.1"
description = "A simple image edit"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Brandon Chew", email = "chewbrandon9911@gmail.com"}
]
dependencies = []

[project.scripts]
SM_Notepad = "Image_Kit:run"
